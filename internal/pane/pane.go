// Package pane is a single terminal instance: it owns a PTY, its VTE state
// machine, the scrollback buffer, and holds input focus.
package pane

// Pane is one concrete terminal instance.
type Pane struct {
	// TODO(stage-1): pty, grid, scrollback ring.
}
