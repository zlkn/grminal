// Package layout implements the LayoutManager: a flat Flexbox-style geometry
// manager (Columns -> Rows) that distributes screen space between panes.
package layout

// Manager assigns screen rectangles to panes for the current tab.
type Manager struct {
	// TODO(stage-3): columns of rows of panes.
}
